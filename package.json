{
	"name": "fami-monorepo",
	"private": true,
	"license": "MIT",
	"author": "pxseu",
	"type": "module",
	"workspaces": [
		"packages/*",
		"examples/*"
	],
	"scripts": {
		"build": "bun run --filter fami build",
		"dev": "bun run --filter fami dev",
		"test": "bun run --filter fami test",
		"typecheck": "bun run --filter fami typecheck",
		"t": "bun run test",
		"tc": "bun run typecheck"
	},
	"devDependencies": {
		"prettier": "^3.8.4"
	}
}
