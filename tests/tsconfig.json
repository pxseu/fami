{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		"lib": ["DOM", "EsNext"],
		"types": ["bun"],
	},
	"include": ["../src/**/*.ts", "./**/*.ts"],
	"exclude": ["../coverage", "../dist", "../examples", "../node_modules"],
}
